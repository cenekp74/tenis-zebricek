# Žebříček tenistů TJ Astra



### Development in windows environment

Run celery on windows: `celery -A app.tasks.celery worker --loglevel=info --pool=solo`

Redis should be started in WSL as a service